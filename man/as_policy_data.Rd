% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{as_policy_data}
\alias{as_policy_data}
\title{Prepare a data frame for longitudinal policy analysis}
\usage{
as_policy_data(data, date, value, drop_missing = FALSE)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{date}{Column containing time values. Supply an unquoted name or a
character string.}

\item{value}{Column containing the measure. Supply an unquoted name or a
character string.}

\item{drop_missing}{If \code{TRUE}, remove rows with missing standardized dates or
values.}
}
\value{
A tibble with \code{date}, \code{value}, and \code{year} as its first columns.
}
\description{
Creates standardized \code{date} and \code{value} columns while retaining the original
fields. Explicit column selection is preferred because policy datasets often
contain several plausible dates and measures.
}
\examples{
raw <- data.frame(fiscal_year = c("FY20", "FY21"), rate = c("51\%", "54\%"))
as_policy_data(raw, fiscal_year, rate)
}
