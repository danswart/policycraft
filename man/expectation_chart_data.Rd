% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expectation.R
\name{expectation_chart_data}
\alias{expectation_chart_data}
\title{Calculate expectation-chart values from moving ranges}
\usage{
expectation_chart_data(
  data,
  date,
  value,
  multiplier = 3,
  d2 = 1.128,
  lower_bound = -Inf,
  upper_bound = Inf
)
}
\arguments{
\item{data}{A data frame containing longitudinal observations.}

\item{date}{Date column. Supply an unquoted name or character string.}

\item{value}{Numeric measure column. Supply an unquoted name or character
string.}

\item{multiplier}{Number of estimated sigma units used for each limit.}

\item{d2}{Bias-correction constant for moving ranges of length two. The
conventional value is \code{1.128}.}

\item{lower_bound}{Optional natural lower bound, such as \code{0} for counts.}

\item{upper_bound}{Optional natural upper bound, such as \code{100} for percentage
points.}
}
\value{
A tibble ordered by date with \code{center}, \code{upper_limit}, \code{lower_limit},
\code{moving_range}, and \code{outside_limits} columns. Calculation details are also
stored in the \code{expectation} attribute.
}
\description{
Calculates an untrended center line and upper and lower expectation limits
from sequential moving ranges. The observations remain in temporal order.
Limits describe the observed system under the selected baseline; they are not
specification limits, confidence intervals, targets, or proof of causation.
}
\examples{
x <- data.frame(year = 2015:2024, measure = c(10, 11, 9, 12, 10, 11, 10, 19, 20, 21))
expectation_chart_data(x, year, measure)
}
