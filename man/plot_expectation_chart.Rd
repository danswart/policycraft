% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-expectation.R
\name{plot_expectation_chart}
\alias{plot_expectation_chart}
\title{Plot a standardized expectation chart}
\usage{
plot_expectation_chart(
  data,
  date,
  value,
  run_length = 8L,
  title = "Expectation Chart",
  subtitle = "Moving-range expectation limits",
  caption = "Signals warrant investigation; they do not identify causes.",
  x_label = "Time Period",
  y_label = "Value",
  ...
)
}
\arguments{
\item{data}{A data frame containing longitudinal observations.}

\item{date}{Date column. Supply an unquoted name or character string.}

\item{value}{Numeric measure column. Supply an unquoted name or character
string.}

\item{run_length}{Minimum run length passed to \code{\link[=detect_runs]{detect_runs()}}. Set to
\code{NULL} to omit run signals.}

\item{title, subtitle, caption}{Plot labels. Use \code{NULL} to omit a label.}

\item{x_label, y_label}{Axis labels.}

\item{...}{Additional arguments passed to \code{\link[=expectation_chart_data]{expectation_chart_data()}}, such
as \code{lower_bound}, \code{upper_bound}, or \code{multiplier}.}
}
\value{
A \code{ggplot} object. Its plotting data include \code{run_signal} and
\code{signal} columns, making the result straightforward to inspect or extend
with additional ggplot2 layers.
}
\description{
Creates a console-friendly expectation chart using the same moving-range
calculations as \code{\link[=expectation_chart_data]{expectation_chart_data()}}. Points outside the expectation
limits, and points completing a run on one side of the center, are
highlighted for investigation. Signals describe unusual patterns in the
observed series; they do not establish a cause or prescribe an action.
}
\examples{
x <- data.frame(
  year = 2015:2024,
  measure = c(10, 11, 9, 12, 10, 11, 10, 19, 20, 21)
)
plot_expectation_chart(x, year, measure)
}
