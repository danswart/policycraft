% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-app.R
\name{launch_longitudinal}
\alias{launch_longitudinal}
\alias{load_longitudinal}
\alias{run_policycraft}
\alias{run_app}
\title{Launch the longitudinal-analysis tool}
\usage{
launch_longitudinal(
  launch_browser = getOption("shiny.launch.browser", interactive()),
  host = getOption("shiny.host", "127.0.0.1"),
  port = getOption("shiny.port", NULL),
  max_upload_mb = 50,
  ...
)

load_longitudinal(
  launch_browser = getOption("shiny.launch.browser", interactive()),
  host = getOption("shiny.host", "127.0.0.1"),
  port = getOption("shiny.port", NULL),
  max_upload_mb = 50,
  ...
)

run_policycraft(...)

run_app(...)
}
\arguments{
\item{launch_browser}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}}. Use \code{TRUE} to open the
system browser, \code{FALSE} to run without opening a browser, or supply a
custom browser function.}

\item{host}{Host address on which to serve the application. The default is
Shiny's local-only default.}

\item{port}{Optional TCP port. \code{NULL} asks Shiny to choose an available port.}

\item{max_upload_mb}{Maximum permitted upload size in megabytes. The default
is 50 MB. The previous \code{shiny.maxRequestSize} option is restored when the
application closes.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Invisibly returns the value produced by \code{\link[shiny:runApp]{shiny::runApp()}}. The
function is normally called for its side effect of starting the app.
}
\description{
Starts policycraft's interactive longitudinal-analysis tool. The application
accepts CSV, Excel, and RDS files and provides mapping, filtering,
longitudinal charts, expectation charts, cohort views, and export controls.
}
\section{Deprecated names}{

\code{run_policycraft()} and \code{run_app()} are retained temporarily for code written
before policycraft became a multi-tool package. New code should use
\code{launch_longitudinal()} (or its \code{load_longitudinal()} alias).
}

\examples{
if (interactive()) {
  launch_longitudinal()
}
}
